<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nju.edu.erp.dao.WarehouseInputListDao">

    <insert id="saveBatch" useGeneratedKeys="true" keyProperty="id">
        insert into warehouse_input_list (wi_id, pid, quantity, purchase_price, production_date, remark) values
        <foreach collection="list" item="item" index="index" separator=",">
            (#{item.wiId},#{item.pid},#{item.quantity},#{item.purchasePrice},#{item.productionDate},#{item.remark})
        </foreach>
    </insert>

</mapper>